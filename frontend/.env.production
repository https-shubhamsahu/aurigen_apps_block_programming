# Baked into the production bundle by Vite at build time.
# These two are PUBLIC by design (Supabase anon key is protected by RLS);
# the service-role key must never appear here.
VITE_SUPABASE_URL=https://voxuhqnntdaohkcyevom.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZveHVocW5udGRhb2hrY3lldm9tIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODMwMTM2OTYsImV4cCI6MjA5ODU4OTY5Nn0.67mrgjFjPf-0O5mz7IVq6Prsv_zf8m4O8rz4Qwq-kdY
# Compile service URL — the Docker backend behind a Cloudflare quick tunnel.
# Stable as long as the cloudflared process keeps running; re-mint with
# `cloudflared tunnel --url http://localhost:4000` and update here if it dies.
VITE_COMPILE_API=https://track-fighters-michel-ethernet.trycloudflare.com
